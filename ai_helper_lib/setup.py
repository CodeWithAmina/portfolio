from setuptools import setup, find_packages

setup(
    name="ai_helper",
    version="0.1.0",
    description="A helper library for AI tasks in Flask apps",
    author="Amina",
    packages=find_packages(),
    install_requires=[],
)
